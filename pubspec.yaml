name: core
description: core package
version: 0.0.4
author: xionghao
homepage: https://github.com/xionghaoo/flutter_core

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fluttertoast: ^3.1.3
  # 下拉刷新
  pull_to_refresh: ^1.5.7
  # 本地文件夹目录
  path_provider: ^1.6.0
  # 日志
  logger: ^0.8.3
  # 网络请求
  dio: ^3.0.9
  crypto: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  assets:
    - images/
